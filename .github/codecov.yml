coverage:
  status:
    project:
      default:
        target: 30%
    patch:
      calendar: 
        target: 0%
        threshold: 0.01%
        flags:
          - calendar
      store:
        target: 100%
        threshold: 0.01%
        flags:
          - store

comment:
  layout: "diff, flags, files"

flags: 
  calendar:
    carryforward: false
    paths:
      - src/components
  store:
    carryforward: false
    paths:
      - src/mst/store

github_checks:
  annotations: true